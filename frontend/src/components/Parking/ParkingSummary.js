import classes from './ParkingSummary.module.css';

const ParkingSummary = props => {
    return (
        <section className={classes.summary}>
            <h2>The Marvelous List of Parking Area</h2>
        </section>
    );
}

export default ParkingSummary;